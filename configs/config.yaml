defaults:
  - _self_

data:
  path: "/home/ma/kausik/workspace/datasets/ImageNet"

train:
  lr: 0.0001
  batch_size: 16
  epochs: 50
  timesteps: 1000
  use_gpu_if_available: true

val:
  freq: 1

wandb:
  enable: false
  project: "diT"
  run_name: "tmp"


model:
  dim: 768
  depth: 12
  patch_size: 2

dataset:
  name: imagenet
  img_size: 256

logging:
  exp_name: "exp_dit"
